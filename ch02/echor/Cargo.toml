[package]
name = "echor"
version = "0.1.0"
edition = "2024"

[dependencies]
# 커맨드 라인 인자 파싱 라이브러리
clap = "4.6.7"

[dev-dependencies]
# 오류 처리 간편화
anyhow = "1.0.104"
# 테스트를 위한 명령어 실행
assert_cmd = "2.2.2"
# 프로그램의 출력을 확인
predicates = "3.1.4"
# 테스트를 보기 좋게 표시
pretty_assertions = "1.4.1"
